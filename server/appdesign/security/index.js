/**
 * Definition der Standard-Security-Einstellung für
 * ein neues Feld innerhalb der Moduldefinition
 */
export const defaultSecurityLevel = {
    visibleBy: ['EVERYBODY'], 
    editableBy: ['ADMIN', 'BACKOFFICE']
}
